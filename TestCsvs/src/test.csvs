load variableName "./path/to/file.csv"; // load a csv
store variableName "./path/to/file.csv"; // save csv
create variableName; // create an empty csv
export variableName "./path/to/file.json"; // export csv to json

// declare and initialize a variable
variableName = "value";
variableName = variable2;
variableName = 123;
variableName = 2 < 4;
cell variableName (123, 456) = 789;

// print values to stdout
print "value";
print variableName;
print 123;
print 2 < 4;

// selectors
print (row variableName (123));
print (col variableName (123));
print (cell variableName (123, 456));
print (field variableName (123));

// delete
delete (row variableName (123));
delete (col variableName (123));

// add
add col variableName;
add row variableName;

on variableName filter (x: x / 2 == 0) apply (x : x * 2);


i = 10;
while(i < 10) {
	i = i + 1;
}
if(i == 10) {
	print "ten!";
}
else {
	print "not ten.";
}

